[pytest]
DJANGO_SETTINGS_MODULE = adminstudio_django.settings
python_files = tests.py test_*.py *_tests.py
addopts = -ra
# Restrict test discovery to the apps directory (where our tests live)
testpaths = apps
