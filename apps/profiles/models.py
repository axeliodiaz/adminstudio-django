# This app only contains views and URLs for the profile endpoint.
# The actual profile data is stored in the User model (apps.users.models.User).
# The Profile model has been removed as all data is now in User.
